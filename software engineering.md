# Project Brief 

`Notion 或 Obsidian`

1 做：先和客户沟通，询问一些问题。如

- 想解决什么问题？
- 现在怎么做的？
- 想实现什么？
- 成功的指标是什么？
- 谁使用的？

上面这些都是很顺着就问下来的

2 整理成文档Project brief

| 字段       | 对应的问题                         |
| ---------- | ---------------------------------- |
| 背景       | 现在面临什么问题？为什么现在要做？ |
| 现状       | 现在怎么解决的？有什么痛点？       |
| 目标       | 解决后想达到什么状态？             |
| 用户       | 谁会用？他们的技术水平？           |
| 成功指标   | 怎么判断成功？（最好有数字）       |
| 范围       | 这次做什么？                       |
| **非范围** | **这次不做什么？**（最重要的一栏） |
| 约束       | 预算、时间、技术栈限制             |

# User Story

`Linear` `FigJam, Miro`

目的是把用户描述的问题，分解成一个一个功能，再一个一个功能的细节

先画 **User Journey**（用户旅程图）→ 再拆 **User Story**

User Journey 是从用户视角，把"完成一件事"的完整流程串起来。比如一个报销系统：

```
登录 → 创建报销单 → 上传发票 → 填金额 → 提交 → 等审批 → 收到通知 → 看到打款
```

这是一条**主流程**。然后每个节点下面才拆 **User story**：

- "上传发票"节点 → 
  - "作为员工，我想拍照上传发票，以便不用手动输入"
  - "作为员工，我想看到 OCR 识别后的金额，以便核对"
  - "作为员工，我想把 OCR 识别后的金额默认填进去"
  - "作为员工，我想把把 OCR 识别到的过大的金额加上提示"

一个产品可能有多个角色，每个用户角色有自己的流程。Payroll系统：

```
登录 → 统计所有员工工时 → 导入系统计算工资 → 审批 → 发放工资
```

- ""统计所有员工工时" 节点→ 
  - 作为员工，我想把所有员工工作的时间按个按钮直接计算出来，不用手动一个一个算
  - 作为员工，我想根据部分把员工的工时分开
  - 作为员工，我想把每个员工的YTD的工时也都列出来做参考

上面的User Journey，横着铺开是它的节点。第一行就是User Journey了

每个节点下面，是具体的功能需求，也就是User Story。

这两者组成了**Story Mapping**

```
报销
 |
登录 → 创建报销单 → 上传发票 → 填金额 → 提交 → 等审批 → 收到通知 → 看到打款
 |			|		|		|		|		|		|		|
邮箱登录  表单生成  拍照上传  输入框	  按钮   流程显示 账户内通知 显示交易号	=== MVP
 |					|		|		|				|		
创建账号			OCR识别  OCR填写  预览			邮件通知
 |							|
修改密码					outlier提示
 |					
忘记密码			
 |					
谷歌登录			
```

- 先写User journey
- 再写Story
- Story优先级，切MVP

MVP Minimum Viable Product 就是最小可用产品

横着切，比如第二行，从邮箱登录--- 显示交易号，他们串起来就是可以工作的一个产品，只不过功能点不丰满

再切一下，就是第二行，V1.2，有更多的功能加进去，更人性化等

再切，就是下一个版本等，当然不一定平行切过去，可以把不是一行的也切进去。当然第一次MVP必须可以串通整个流程

# WBS

WBS + 三点估算

**WBS Work Breakdown Structure**

- Story Mapping 是**用户视角**——"用户要做什么"
- WBS 是**实现视角**——"我要建造什么"

```
在线课程平台
├── 1. 用户系统
│   ├── 1.1 注册
│   │   ├── 1.1.1 邮箱注册接口
│   │   ├── 1.1.2 注册页面
│   │   └── 1.1.3 邮箱验证
│   └── 1.2 登录
│       ├── 1.2.1 登录接口
│       └── 1.2.2 登录页面
├── 2. 课程系统
│   ├── 2.1 课程展示
│   └── 2.2 课程购买
└── 3. 学习系统
    └── ...
```

- WBS是开发人员实现的功能
- 一个User Story可能对应多个WBS任务节点
- 有些可能没有在User Story中比如测试，CI/CD，脚手架这些

# 估算时间

`Toggl, Clockify`

**三点估算**

把项目拆成不超过 1–2 天就能完成的任务。每个任务给三个数：

- O（乐观，一切顺利）
- R（现实，正常情况）
- P（悲观，踩坑了）

期望值 = (O + 4R + P) / 6

举例："实现发票 OCR 上传"：O=4h, R=8h, P=20h（踩坑可能是 OCR 库装不上、识别率差需要换方案）→ 期望 = (4 + 32 + 20) / 6 ≈ 9.3h

把所有任务加起来，**再乘 1.3–1.5 的 buffer** 给"未知的未知"。



**高级方法，按接口算，需要持续记录工作时间，统计得来**

估算的几个铁律：

1. **追踪实际工时 vs 估算工时**。用 Toggl 之类的工具，结束后看差异，下次校准。
2. **估算单位用半天或天，别用小时**。小时级别的精度是假精度。
3. **告诉客户的工期 = 你估算的工期 × 1.5**。这不是骗，是保留处理意外的空间。如果提前做完，客户会很高兴；如果超期，灾难。
4. **永远不要在客户问"几天能搞定？"时当场回答**。说"我评估一下，明天给你"。当场报的数字 90% 是错的。



# 线框图 Wireframe

`Excalidram, draw.io, figma`

**代码原型（Code Prototype）**

内部使用功能 `Excalidram`

- 画低保真线框图
- 代码实现
- 现成组件库
- 演示

**设计原型（Design Prototype）**

面向客户，UI也是工程 `figma`

- 设计工具里做，不写代码
- 设计，字体，位置，间距精确
- 用户体验好
- 成本高



# 技术决策

**ADR Architecture Decision Records**

记录当时选择技术的考量，特别是因为X，Y，Z而选择的某个技术

1. 之后知道为什么这么做
2. 当需要改变的时候，看改变后X，Y，Z是否还成立

用简单md记录一下就行





# 测试，用户反馈

一个是做好一个Story之后，一个流程后，**给客户演示**

循环：

- 完成上周的反馈意见
- 做完一个Story
- 录Loom
  - 开头总结完成什么
  - 演示
  - 提问（得到反馈和反应，否则没回信了）

MVP完成，演示，修改，完成一个Version

注意：

- 提前告诉客户"这次重点看 X，Y 还没做"，否则客户会盯着你没做完的 Y 提一堆意见



一个是**自己的测试**

- 单元测试（关键逻辑）
- 自己手动跑一遍用户流程
- 边界情况测试（空数据、超长输入、网络断开）
- 覆盖率

# 交付 & 复盘

部署文档、用户文档、内部文档（给未来的自己）都要写

做一次 **Retrospective**：哪里超时、哪里估错、下次怎么改。这是让你越做越快的关键。